# 随笔排版策略

本仓库 `docs/essays/` 下所有中英文随笔统一按本文件排版。新文直接套用；改旧文以本文件为准。

## 1. 文件与命名

- 路径：`docs/essays/`
- 文件名：`YYMMDDNN-slug.zh.md` / `YYMMDDNN-slug.en.md`
- 中英成对出现时，章节结构、分隔、列表与表格应对齐（可有必要的语言差异，不可一边有表一边没有、一边有结语一边没有）

## 2. Frontmatter

```yaml
---
title: "文章标题"
date: YYYY-MM-DD
lang: zh-CN   # 英文用 en
---
```

- `title` 必须与正文一级标题 `#` **完全一致**
- `title` 即导航、首页、Essays 列表使用的显示名（勿另起一套短标题）

## 3. 正文骨架

```markdown
# 文章标题

导语（1–3 段，提出问题或总论）

---

## 第一节标题

段落或列表……

---

## 第二节标题

……

---

## 结语

收束全文。

---

<!--@include: ../snippets/_author-license.md-->
```

硬性约定：

| 项目 | 规则 |
| --- | --- |
| 一级标题 | 全文唯一一个 `#`，与 `title` 相同 |
| 导语 | `#` 之后直接写 1–3 段；**不要**在导语前插 `---` |
| 章节 | 只用 `##`；长文内再分用 `###` |
| 章节名 | **描述性标题**，不用「一、二、三」或「I. II. III.」编号 |
| 章间分隔 | 上一节正文结束后空一行，写 `---`，再空一行，写下一节 `##` |
| 多余空行 | 禁止 `---` 与 `##` 之间出现两个及以上空行 |
| 结语 | 中文固定为 `## 结语`；英文固定为 `## Closing`（不要写成「八、结论：…」） |
| 文末 | 结语后 `---`，空一行，再写 author/license 的 `@include` |

## 4. 强调、引号与术语

- 关键命题、对比项、定义标签用 `**加粗**`
- 中文引用、提及概念用「直角引号」，不用英文 `"双引号"`
- 英文可用 `"..."` 或破折号，保持篇内一致
- 中文正文里的拉丁缩写两侧留空格：`AI`、`DNA`、`BCI` 等（句首除外按汉语习惯）
- 不要用 emoji

## 5. 列表与表格

**定义型列表**（标签 + 解释）统一为：

```markdown
- **标签：** 解释文字……
```

**主体型列表**（加粗词作主语，后面直接接谓语）可用：

```markdown
- **硅基智能**可以……
- **人脑**拥有……
```

- 同一节内只选一种列表风格，不要混用
- 有对比维度时用 GitHub Flavored Markdown 表格；表前表后各空一行
- 表格列对齐用 `| --- |`，表头简明

## 6. 中英对照

- 英文章节标题翻译中文章节标题，不另起一套结构
- 导航与 `docs/index.md`、`docs/essays/index.md`、`docs/.vitepress/config.ts` 中英成对链接
- 文末 license 块只通过 `docs/snippets/_author-license.md` 引入，不在单篇里复制粘贴

## 7. 检查清单（发布前）

- [ ] `title` === `#` 标题
- [ ] 有导语；导语前无 `---`
- [ ] 章节为描述性 `##`，无「一、」「I.」编号
- [ ] 章间均为：空行 + `---` + 空行（仅一空行）
- [ ] 最后一节为 `结语` / `Closing`
- [ ] 文末 `@include` 正确
- [ ] 中文无 ASCII `"..."` 引号；无 emoji
- [ ] 若有英文版，结构平行，且索引/sidebar 已挂上
